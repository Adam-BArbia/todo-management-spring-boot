<!DOCTYPE html>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c"%>
<%@ taglib uri="http://java.sun.com/jsp/jstl/fmt" prefix="fmt"%>
<%@taglib uri="http://www.springframework.org/tags/form" prefix="form"%>


<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Todo Management</title>
	<link href="${pageContext.request.contextPath}/webjars/bootstrap/3.3.6/css/bootstrap.min.css" rel="stylesheet">
	<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">

	<style>
		:root {
			--theme-blue: #0b2340;
			--theme-blue-dark: #062033;
			--theme-blue-mid: #123b67;
			--theme-blue-soft: #edf4ff;
			--theme-border: #dbe3ef;
		}

		body {
			margin: 0;
			padding: 0;
			padding-top: 80px; /* space for fixed navbar */
			background-color: #f8fbff;
			color: #222;
			font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
		}

		.footer {
			position: fixed;
			left: 0;
			bottom: 0;
			width: 100%;
			background-color: black;
			color: white;
			height: 50px;
			text-align: center;
			padding: 8px 0;
			border-top: 1px solid #333;
			font-size: 12px;
			z-index: 999;
			display: flex;
			align-items: center;
			justify-content: center;
		}

		body > *:not(.footer) {
			padding-bottom: 66px;
		}

		table tbody td {
			vertical-align: middle;
		}

		.panel-primary {
			border-color: var(--theme-blue);
			box-shadow: 0 8px 20px rgba(11, 35, 64, 0.08);
		}
		.panel-primary > .panel-heading {
			background-color: var(--theme-blue);
			border-color: var(--theme-blue);
			color: #fff;
			font-weight: 600;
		}

		.btn-primary {
			background-color: var(--theme-blue);
			border-color: var(--theme-blue-dark);
		}
		.btn-primary:hover,
		.btn-primary:focus,
		.btn-primary:active,
		.btn-primary.active,
		.open > .dropdown-toggle.btn-primary {
			background-color: var(--theme-blue-mid);
			border-color: var(--theme-blue-dark);
		}

		.btn-info {
			background-color: var(--theme-blue-mid);
			border-color: var(--theme-blue);
		}
		.btn-info:hover,
		.btn-info:focus,
		.btn-info:active,
		.btn-info.active,
		.open > .dropdown-toggle.btn-info {
			background-color: var(--theme-blue);
			border-color: var(--theme-blue-dark);
		}

		.badge,
		.label-primary {
			background-color: var(--theme-blue);
		}

		.alert-info {
			background-color: var(--theme-blue-soft);
			border-color: var(--theme-border);
			color: #1f3a5a;
		}

		/* Navbar tweaks */
		.navbar-inverse {
			background-color: var(--theme-blue);
			border-color: var(--theme-blue-dark);
			padding: 5px 0 !important;
			min-height: auto !important;
		}
		.navbar-inverse .navbar-brand {
			color: #fff;
			font-weight: 600;
			letter-spacing: 0.5px;
			padding: 5px 15px !important;
		}
		.navbar-inverse .navbar-nav>li>a {
			color: #dbe9ff;
			padding: 5px 12px !important;
		}
		.navbar-inverse .navbar-nav>li>a:hover {
			color: #ffffff;
		}
		.navbar-header {
			padding: 5px 0 !important;
		}

		.input-date-wrapper { position: relative; }
		.real-date-input { position: relative; z-index: 2; }
		.real-date-input:not(:focus) { color: transparent; }
		.real-date-input::placeholder { color: #999; }

		.date-overlay {
			position: absolute;
			left: 12px;
			top: 50%;
			transform: translateY(-50%);
			color: #555;
			font-style: italic;
			z-index: 1;
			pointer-events: none;
			white-space: nowrap;
			overflow: hidden;
			text-overflow: ellipsis;
		}
	</style>
</head>
<body>